#!/bin/bash

# This script configured git whenever you start working on multiple machines.
# Run it any time you decide to work with it on a new computer

git config --global user.name "Conrad Pearson"
git config --global user.email "cspearso@calpoly.edu"
git config --global color.ui "auto"
git config --global core.editor  "nano"
